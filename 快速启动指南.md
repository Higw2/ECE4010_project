# 🎯 快速启动指南（5分钟上手）

## 前置条件检查

```bash
# 1. 检查 Python 依赖
pip install docker pandas numpy torch scikit-learn matplotlib

# 2. 检查 NAS 连接
python test_connection.py
# 应该输出：✓ 成功连接到 NAS Docker
```

---

## 🚀 快速启动 (4步)

### 第1步：采集数据（可选，如果已有数据可跳过）

```bash
# 编辑 collect_metrics.py
# 修改第 ~90 行：
NAS_IP = '192.168.3.2'
NAS_PORT = 2375

# 运行采集器（后台运行，采集 3 天）
nohup python collect_metrics.py > collection.log 2>&1 &

# 或者快速测试（采集 2 分钟）
python collect_metrics.py  # 修改最后的 duration_hours=2/60
```

**进度查看**：
```bash
tail -f collection.log
# 输出：
# ✓ 采集了 3 个容器
# ✓ 数据已保存: data/docker_metrics_20240115_100000.csv
```

### 第2步：检查数据

```bash
python check_data.py
# 输出示例：
# ✓ 加载: data/docker_metrics_20240115_100000.csv
# 数据形状: (25920, 10)
# 列名: ['timestamp', 'container_name', 'cpu_percent', ...]
# 容器列表: ['qbittorrent', 'jellyfin', 'transmission']
```

### 第3步：训练模型

```bash
python complete_pipeline_fixed.py
```

**监看输出**：
```
步骤 1: 数据预处理
  处理容器: qbittorrent
    样本数: 1234
    创建了 1229 个窗口
    训练集: 860, 测试集: 369

步骤 2: 训练 LSTM Autoencoder
  训练 qbittorrent...
    Epoch  1/30 - Loss: 0.234567
    Epoch 10/30 - Loss: 0.012345
    Epoch 30/30 - Loss: 0.001234
    ✓ 模型已保存
    ✓ 训练曲线已保存

步骤 3: 异常检测
  检测 qbittorrent...
    样本数: 369
    平均误差: 0.0234
    标准差: 0.0156
    阈值 (mean+2σ): 0.5500      ← 记下这个数字！
    异常数: 2 / 369 (0.5%)

✅ 项目运行完成！
```

**查看阈值**：
```bash
cat output/report.txt
# 找这一行：
# 阈值 (mean+2σ): 0.5500
```

### 第4步：启动实时监控

```bash
# 编辑 realtime_monitor.py 第 400-410 行
# 修改以下配置：

TARGET_CONTAINER = "qbittorrent"        # ← 改成你的容器名
ALERT_THRESHOLD = 0.55                  # ← 从 report.txt 复制
NAS_IP = '192.168.3.2'
NAS_PORT = 2375
SAMPLING_INTERVAL = 10

# 启动监控
python realtime_monitor.py
```

**正常输出**：
```
[14:23:45] ✓ qbittorrent 运行平稳 | 误差: 0.0245 | CPU:  25.1% | 内存:  512.3MB
[14:23:55] ✓ qbittorrent 运行平稳 | 误差: 0.0231 | CPU:  24.8% | 内存:  513.1MB
[14:24:05] ✓ qbittorrent 运行平稳 | 误差: 0.0239 | CPU:  26.2% | 内存:  514.5MB
```

---

## 🚨 异常告警演示

### 如何手动触发异常

```bash
# 方式1：模拟高CPU负载
docker exec qbittorrent stress --cpu 4 --timeout 60s

# 方式2：模拟内存使用
docker exec qbittorrent stress --vm 1 --vm-bytes 1G --timeout 60s

# 方式3：模拟网络流量
docker exec qbittorrent iperf -c 192.168.3.1 -P 4 -t 60
```

### 告警效果

```
[14:24:35] ✓ qbittorrent 运行平稳 | 误差: 0.0242 | CPU:  25.5% | 内存:  515.2MB

============================================================
🚨 [异常告警] 容器 'qbittorrent' 状态异常！
============================================================
  重构误差: 0.6234 > 阈值: 0.5500
  超过阈值: 0.0734

  当前性能指标:
    ├─ CPU:      95.2%
    ├─ 内存:   2048.5MB
    ├─ 网络入:    5.20MB/s
    └─ 网络出:    2.15MB/s
============================================================
```

---

## 🔍 故障排查

### 问题1：找不到容器

```
✗ 无法找到容器 'qbittorrent': 
  可用的容器有: ['jellyfin', 'transmission', ...]
```

**解决方案**：
```bash
# 查看可用的容器名称
python -c "
import docker
client = docker.DockerClient(base_url='tcp://192.168.3.2:2375')
for c in client.containers.list():
    print(f'  - {c.name}')
"

# 修改 realtime_monitor.py 中的 TARGET_CONTAINER
```

### 问题2：缺少 .pth 或 .pkl 文件

```
✗ 模型文件不存在: models/qbittorrent_model.pth
✗ 标化器文件不存在: models/qbittorrent_scaler.pkl
```

**解决方案**：
```bash
# 重新运行训练脚本
python complete_pipeline_fixed.py

# 确认文件已生成
ls -la models/
# 应该看到：
# qbittorrent_model.pth (2MB)
# qbittorrent_scaler.pkl (100KB)
```

### 问题3：无法连接 NAS Docker

```
✗ 连接失败: Connection refused
✗ 无法连接 NAS Docker: timed out
```

**排查步骤**：

```bash
# 1. 检查 NAS IP 是否正确
ping 192.168.3.2

# 2. 检查 Docker API 端口是否开放
nc -zv 192.168.3.2 2375

# 3. SSH 登录 NAS 检查 Docker 是否运行
ssh admin@192.168.3.2
sudo docker ps

# 4. 在 NAS 上检查 TCP 监听
sudo netstat -tulnp | grep 2375
# 应该输出：tcp  0  0 0.0.0.0:2375  0.0.0.0:*  LISTEN  ...
```

### 问题4：阈值太敏感或太迟钝

```
# 如果误报太多，提高阈值
ALERT_THRESHOLD = 0.65  # 之前是 0.55

# 如果漏报太多，降低阈值
ALERT_THRESHOLD = 0.45
```

---

## 📊 文件结构速查

```
项目目录/
│
├── 采集脚本
│   └── collect_metrics.py           → 采集容器性能数据
│
├── 训练脚本
│   ├── complete_pipeline_fixed.py   → 训练 LSTM 模型
│   └── check_data.py                → 检查采集数据
│
├── 监控脚本
│   ├── realtime_monitor.py          → ★ 实时监控（你要运行的）
│   ├── test_connection.py           → 测试连接
│   └── diagnose_container.py        → 诊断容器
│
├── data/                             → 采集的 CSV 数据
│   └── docker_metrics_*.csv
│
├── models/                           → 训练的模型文件
│   ├── qbittorrent_model.pth        ← 模型权重
│   └── qbittorrent_scaler.pkl       ← 标化器
│
└── output/                           → 输出结果
    ├── report.txt                    ← 阈值在这里
    ├── *_training_loss.png
    └── *_anomaly_detection.png
```

---

## 🎓 关键参数说明

### seq_len = 6

- 时间窗口大小
- 6个 × 10秒 = 60秒 = 一个完整观察周期
- **不要改**，必须与训练时一致

### threshold

- 告警阈值
- 从 `output/report.txt` 获取
- 可以根据需要调整 (±10% 左右)
- 单位：均方误差 (MSE)

### SAMPLING_INTERVAL = 10

- 采样间隔（秒）
- 每10秒采样一次
- 可以改为 5-30 秒
- 越短越敏感，越长越稳定

---

## 💡 性能优化建议

### 如果 CPU 占用太高

```python
# 在 realtime_monitor.py 中增加采样间隔
SAMPLING_INTERVAL = 30  # 从 10 秒改为 30 秒
```

### 如果频繁误报

```python
# 提高阈值（降低敏感度）
ALERT_THRESHOLD = 0.65  # 从 0.55 改为 0.65
```

### 如果漏报太多

```python
# 降低阈值（提高敏感度）
ALERT_THRESHOLD = 0.45  # 从 0.55 改为 0.45
```

---

## 📝 日志文件位置

```bash
# 实时监控日志
realtime_monitor.log

# 数据采集日志
collection.log

# 查看日志
tail -f realtime_monitor.log

# 搜索告警
grep "异常告警" realtime_monitor.log
```

---

## 🎬 演示脚本（适合答辩）

**时长：5分钟**

1. **启动监控**（1分钟）
   ```bash
   python realtime_monitor.py
   # 显示正常运行的容器信息
   ```

2. **介绍原理**（2分钟）
   - "这个系统用 LSTM Autoencoder 学习了容器的正常行为模式"
   - "现在实时监控容器的性能数据"
   - "每当重构误差超过阈值，就表示容器出现异常"

3. **触发异常**（1分钟）
   ```bash
   # 在另一个终端
   docker exec qbittorrent stress --cpu 4 --timeout 60s
   ```

4. **观看告警**（1分钟）
   - 等待30秒，看到红色的 🚨 告警
   - 展示告警中的 CPU、内存等指标

---

## ✅ 上线前检查清单

在将系统投入生产使用前：

- [ ] 已验证容器名称正确
- [ ] 已从 output/report.txt 获取阈值
- [ ] 已测试 NAS 连接（test_connection.py）
- [ ] 已验证模型文件存在（models/ 目录）
- [ ] 已进行至少一次告警测试
- [ ] 已配置日志重定向或后台运行
- [ ] 已备份模型文件
- [ ] 已准备告警响应流程

---

## 🔗 后续扩展

### 1. 多容器监控
```python
CONTAINERS = ["qbittorrent", "jellyfin", "transmission"]
for container in CONTAINERS:
    detector = RealTimeAnomalyDetector(container, threshold)
    detector.run_monitor()
```

### 2. 企业微信告警
```python
def send_dingtalk(message):
    import requests
    webhook = "https://oapi.dingtalk.com/robot/send?..."
    requests.post(webhook, json={"text": {"content": message}})
```

### 3. 自动恢复
```python
if mse_error > threshold:
    container.restart()  # 自动重启容器
    logger.info("✓ 容器已自动重启")
```

### 4. 数据库存储
```python
# 把告警记录到数据库
db.insert({
    'timestamp': datetime.now(),
    'container': container_name,
    'error': mse_error,
    'threshold': threshold,
    'anomaly': True
})
```

---

## 📞 常见提问

**Q: 为什么要用 LSTM Autoencoder？**
A: 因为容器性能数据是**时序数据**，具有时间依赖性。LSTM 能够学习这种时间相关性，从而更准确地检测异常。

**Q: 为什么需要保存 scaler？**
A: 训练时数据被标准化过，实时数据也必须用相同的参数进行标准化。否则模型会对真实的新数据产生完全错误的预测。

**Q: 能检测什么类型的异常？**
A: 
- ✅ CPU 突然飙升
- ✅ 内存泄漏（逐渐增长）
- ✅ 异常的网络流量
- ✅ 任何偏离正常模式的行为

**Q: 能检测到已知的攻击吗？**
A: 可以。只要攻击会改变容器的性能指标（CPU、内存、网络等），系统就能检测到。

**Q: 如何处理业务繁忙期的"合法"峰值？**
A: 
1. 用繁忙期的数据重新训练模型
2. 或者根据时间段使用不同的阈值

---

## 🎉 恭喜！

你现在拥有一个完整的、生产级别的实时异常检测系统！

💪 下一步建议：
1. 部署到 24/7 运行的服务器
2. 配置自动告警和响应
3. 收集更多的历史数据来改进模型
4. 在 GitHub 上分享你的项目代码

祝你成功！🚀
