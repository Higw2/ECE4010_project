# 🏗️ 系统架构设计与技术细节

## 整体架构图

```
┌─────────────────────────────────────────────────────────────────────┐
│                        NAS (绿联 DH4300 Plus)                         │
│  ┌──────────────────────────────────────────────────────────────┐   │
│  │                    Docker 容器群                             │   │
│  │  ┌──────────────┐  ┌──────────────┐  ┌──────────────┐      │   │
│  │  │ qbittorrent  │  │  jellyfin    │  │transmission  │      │   │
│  │  │ CPU: 25%     │  │ CPU: 10%     │  │ CPU: 5%      │      │   │
│  │  │ Mem: 512MB   │  │ Mem: 256MB   │  │ Mem: 128MB   │      │   │
│  │  └──────────────┘  └──────────────┘  └──────────────┘      │   │
│  │         ↓                ↓                ↓                   │   │
│  │  ┌──────────────────────────────────────────────────────┐   │   │
│  │  │    Docker Daemon (监听 0.0.0.0:2375)                 │   │   │
│  │  └──────────────────────────────────────────────────────┘   │   │
│  └──────────────────────────────────────────────────────────────┘   │
│                           ↓                                           │
│                    Docker Stats API                                  │
│                                                                       │
│                    2375 端口暴露                                      │
└─────────────────────────────────────────────────────────────────────┘
        ↓ TCP (192.168.3.2:2375)
        ↓
┌─────────────────────────────────────────────────────────────────────┐
│                    本地机器 (监控服务器)                              │
│                                                                       │
│  ┌──────────────────────────────────────────────────────────────┐   │
│  │         1. 数据采集阶段 (1-3天)                              │   │
│  │  ┌────────────────────────────────────────────────────────┐ │   │
│  │  │  collect_metrics.py                                    │ │   │
│  │  │  - 每 10 秒轮询一次 Docker API                           │ │   │
│  │  │  - 提取 CPU、内存、网络指标                               │ │   │
│  │  │  - 保存到 CSV 文件                                       │ │   │
│  │  │  ↓                                                       │ │   │
│  │  │  data/docker_metrics_20240115_100000.csv               │ │   │
│  │  │  (25920 行 × 10 列)                                     │ │   │
│  │  └────────────────────────────────────────────────────────┘ │   │
│  └──────────────────────────────────────────────────────────────┘   │
│                           ↓                                           │
│  ┌──────────────────────────────────────────────────────────────┐   │
│  │         2. 模型训练阶段 (5-10分钟)                           │   │
│  │  ┌────────────────────────────────────────────────────────┐ │   │
│  │  │  complete_pipeline_fixed.py                            │ │   │
│  │  │                                                         │ │   │
│  │  │  [预处理]                                               │ │   │
│  │  │    CSV → 特征提取 → 标准化                               │ │   │
│  │  │    [CPU, Mem, NetIn, NetOut]                          │ │   │
│  │  │                                                         │ │   │
│  │  │  [时间窗口化]                                            │ │   │
│  │  │    单个数据点 → 6个连续步长的序列                         │ │   │
│  │  │    [[...], [...], [...], [...], [...], [...]]         │ │   │
│  │  │                                                         │ │   │
│  │  │  [分割数据]                                              │ │   │
│  │  │    70% 训练集 / 30% 测试集                              │ │   │
│  │  │                                                         │ │   │
│  │  │  [训练模型]                                              │ │   │
│  │  │    LSTM Autoencoder, 30 Epochs                         │ │   │
│  │  │    Loss: 0.23 → 0.001                                 │ │   │
│  │  │                                                         │ │   │
│  │  │  [计算阈值]                                              │ │   │
│  │  │    mean_error = 0.0234                                 │ │   │
│  │  │    std_error = 0.0156                                  │ │   │
│  │  │    threshold = 0.0234 + 2*0.0156 = 0.5546             │ │   │
│  │  │                                                         │ │   │
│  │  │  [保存]                                                  │ │   │
│  │  │    ↓                  ↓                    ↓            │ │   │
│  │  │   .pth 文件      .pkl 文件          report.txt         │ │   │
│  │  │  (模型权重)     (标化器参数)       (阈值信息)           │ │   │
│  │  └────────────────────────────────────────────────────────┘ │   │
│  └──────────────────────────────────────────────────────────────┘   │
│                           ↓                                           │
│  ┌──────────────────────────────────────────────────────────────┐   │
│  │    3. 实时监控阶段 (24/7 运行)                              │   │
│  │  ┌────────────────────────────────────────────────────────┐ │   │
│  │  │  realtime_monitor.py                                  │ │   │
│  │  │                                                         │ │   │
│  │  │  ┌────────────────────────────────────────────────┐    │ │   │
│  │  │  │ 主循环                                          │    │ │   │
│  │  │  │                                                 │    │ │   │
│  │  │  │ [1] 获取实时数据                               │    │ │   │
│  │  │  │     raw = get_stats(container)                 │    │ │   │
│  │  │  │     [CPU=25.1%, Mem=512MB, ...]               │    │ │   │
│  │  │  │                                                 │    │ │   │
│  │  │  │ [2] 标准化                                       │    │ │   │
│  │  │  │     scaled = scaler.transform(raw)             │    │ │   │
│  │  │  │                                                 │    │ │   │
│  │  │  │ [3] 加入窗口                                     │    │ │   │
│  │  │  │     window.append(scaled)                       │    │ │   │
│  │  │  │     if len(window) == 6:                        │    │ │   │
│  │  │  │                                                 │    │ │   │
│  │  │  │ [4] 模型推理                                     │    │ │   │
│  │  │  │     output = model(tensor(window))             │    │ │   │
│  │  │  │     error = MSE(input, output)                 │    │ │   │
│  │  │  │                                                 │    │ │   │
│  │  │  │ [5] 判断异常                                     │    │ │   │
│  │  │  │     if error > threshold:                       │    │ │   │
│  │  │  │         发出 🚨 告警                             │    │ │   │
│  │  │  │     else:                                        │    │ │   │
│  │  │  │         print("✓ 运行平稳")                      │    │ │   │
│  │  │  │                                                 │    │ │   │
│  │  │  │ [6] 等待下次采样                                 │    │ │   │
│  │  │  │     time.sleep(10)                              │    │ │   │
│  │  │  │                                                 │    │ │   │
│  │  │  └────────────────────────────────────────────────┘    │ │   │
│  │  │                                                         │ │   │
│  │  │  输出：                                                  │ │   │
│  │  │  ├─ 控制台打印 (实时)                                    │ │   │
│  │  │  ├─ realtime_monitor.log (持久化)                       │ │   │
│  │  │  └─ 告警通知 (邮件/企业微信/etc)                         │ │   │
│  │  │                                                         │ │   │
│  │  └────────────────────────────────────────────────────────┘ │   │
│  └──────────────────────────────────────────────────────────────┘   │
│                                                                       │
└─────────────────────────────────────────────────────────────────────┘
```

---

## 🔄 数据流详解

### 采集阶段的数据流

```
Docker Stats API (每10秒)
        ↓
    {
      "cpu_stats": {...},
      "memory_stats": {...},
      "networks": {...}
    }
        ↓
   解析 + 计算
        ↓
    {
      "timestamp": "2024-01-15T10:00:00",
      "container_name": "qbittorrent",
      "cpu_percent": 25.5,
      "memory_mb": 512.3,
      "net_in_rate_mbs": 0.1,
      "net_out_rate_mbs": 0.05
    }
        ↓
   追加到 Buffer
        ↓
   定期保存到 CSV
```

### 训练阶段的数据流

```
CSV 文件 (2592 行，3天每10秒采样)
        ↓
提取特征 [CPU, Mem, NetIn, NetOut]
        ↓
标准化 (Scaler.fit_transform)
特性：mean=50, scale=10
        ↓
        input[0] = 25.5  →  (25.5 - 50) / 10 = -2.45  (标准化值)
        ↓
创建时间窗口 (seq_len=6)
        ↓
样本 0: [t0, t1, t2, t3, t4, t5]
样本 1: [t1, t2, t3, t4, t5, t6]
...
        ↓
分割数据
        ↓
训练集 (70%)    →  训练 LSTM Autoencoder
测试集 (30%)    →  计算重构误差 & 阈值
        ↓
保存:
├─ models/qbittorrent_model.pth       (模型权重)
├─ models/qbittorrent_scaler.pkl      (mean & scale)
└─ output/report.txt                  (阈值 = 0.5546)
```

### 实时推理阶段的数据流

```
时刻 T: 从 Docker 获取 [CPU=25.1, Mem=512.3, NetIn=0.12, NetOut=0.06]
        ↓
用保存的 scaler 标准化
        input = [25.1, 512.3, 0.12, 0.06]
        scaled = (input - scaler.mean) / scaler.scale
        scaled = [-2.49, 506.3, 0.12, 0.06]
        ↓
加入 deque 窗口
        window = deque(maxlen=6)
        window.append(scaled)
        ↓
窗口满 6 个样本时 (60秒后)
        ↓
        tensor = torch.tensor(window).unsqueeze(0)  # (1, 6, 4)
        ↓
        LSTM Encoder
        ↓
        Latent Code (压缩表示)
        ↓
        LSTM Decoder
        ↓
        output = torch.tensor(...)  # (1, 6, 4)
        ↓
        error = ((tensor - output) ** 2).mean()
        error = 0.0245
        ↓
        if error > 0.5546:
            🚨 异常告警
        else:
            ✓ 正常运行
        ↓
        移除窗口中最旧的样本
        加入最新的样本
        循环继续
```

---

## 🧠 LSTM Autoencoder 架构详解

### 编码器 (Encoder)

```
输入时序数据
shape: (batch=1, seq_len=6, n_features=4)

    [CPU1, Mem1, NetIn1, NetOut1]
    [CPU2, Mem2, NetIn2, NetOut2]
    [CPU3, Mem3, NetIn3, NetOut3]
    [CPU4, Mem4, NetIn4, NetOut4]
    [CPU5, Mem5, NetIn5, NetOut5]
    [CPU6, Mem6, NetIn6, NetOut6]
    
        ↓ LSTM (hidden_size=16)
        ↓ 提取时间依赖性
        
    隐状态 h: [... 16维向量 ...]
    
        ↓ Dense 层 1 (16 → 32)
        ↓ ReLU 激活
        
    中间表示: [... 32维向量 ...]
    
        ↓ Dense 层 2 (32 → 8)
        
    潜在编码 z: [... 8维向量 ...]
    
这个 8 维的向量就是数据的"压缩表示"
- 正常数据 → 潜在空间中的某个区域
- 异常数据 → 潜在空间的陌生区域
```

### 解码器 (Decoder)

```
潜在编码 z
[... 8维向量 ...]

    ↓ Dense 层 (8 → 16)
    
    初始隐状态 h0: [... 16维向量 ...]
    
    ↓ 复制成 6 个时间步
    
    [h0, h0, h0, h0, h0, h0]
    
    ↓ LSTM (hidden_size=16)
    ↓ 逐步还原时间结构
    
    隐状态序列:
    [h_out_1, h_out_2, h_out_3, h_out_4, h_out_5, h_out_6]
    
    ↓ Dense 层 (16 → 4)
    
    输出时序数据
    shape: (batch=1, seq_len=6, n_features=4)
    
    [CPU1', Mem1', NetIn1', NetOut1']
    [CPU2', Mem2', NetIn2', NetOut2']
    [CPU3', Mem3', NetIn3', NetOut3']
    [CPU4', Mem4', NetIn4', NetOut4']
    [CPU5', Mem5', NetIn5', NetOut5']
    [CPU6', Mem6', NetIn6', NetOut6']
```

### 重构误差计算

```
输入: X (原始的 6 个时间步)
输出: X' (模型重构的 6 个时间步)

重构误差 = MSE(X, X')
        = mean((X - X') ^ 2)
        = mean(所有 6×4=24 个元素的平方差的平均值)
        
例如：
X  = [25.5, 512.3, 0.1, 0.05, ...]
X' = [25.2, 511.9, 0.1, 0.05, ...]

error = ((25.5-25.2)^2 + (512.3-511.9)^2 + ...) / 24
      = (0.09 + 0.16 + 0 + 0 + ...) / 24
      = 0.0245

判断：
if error > threshold (0.5546):
    异常！(输入的模式不像训练数据)
else:
    正常 (输入的模式像训练数据)
```

---

## 📊 标化器 (Scaler) 的重要性

### 为什么必须使用相同的 Scaler？

```
训练阶段：
    原始数据：[CPU=25, Mem=512, ...]
    
    scaler.fit(训练数据全集)
    → 计算均值 mean = [50, 1024, ...]
    → 计算标准差 scale = [10, 256, ...]
    
    scaler.transform(原始数据)
    → (25 - 50) / 10 = -2.5
    → (512 - 1024) / 256 = -2.0
    → 标准化结果：[-2.5, -2.0, ...]
    
    模型学到：
    "这个样本 [-2.5, -2.0, ...] 是正常的"
```

### 实时推理阶段的错误示例

❌ **错误做法**（使用不同的 scaler）：

```python
# 实时数据
new_data = [25, 512, ...]

# 如果用不同的 scaler
scaler_wrong = StandardScaler()
scaler_wrong.fit(other_data)  # 这会计算出不同的 mean/scale
result = scaler_wrong.transform([25, 512, ...])
result = [-5.0, -3.0, ...]  # 完全不同！

# 模型看到 [-5.0, -3.0, ...] 时：
# "这不像我学过的任何东西，这是异常！"
# 实际上只是标准化方式不对...

→ 导致所有数据都被认为是异常
```

✅ **正确做法**（使用相同的 scaler）：

```python
# 实时数据
new_data = [25, 512, ...]

# 使用保存的 scaler
with open('models/qbittorrent_scaler.pkl', 'rb') as f:
    scaler = pickle.load(f)

result = scaler.transform([25, 512, ...])
result = [-2.5, -2.0, ...]  # 与训练时完全一样！

# 模型看到 [-2.5, -2.0, ...] 时：
# "这就是我学过的正常模式"
# 低误差，判断为正常

→ 正确的异常检测
```

---

## 🕐 时间窗口的工作原理

### 为什么需要时间窗口？

```
情景 1：单点预测（错误的方法）

时刻 T：CPU = 95%
单点输入模型：[95]
模型困惑：
  "这是异常吗？"
  "但是我是在学习 6 个时间步的模式..."
  "我不知道怎么只用 1 个点来判断"

→ 性能很差


情景 2：时间窗口（正确的方法）

时刻 T-50秒：CPU = 25%, Mem = 512MB, ...
时刻 T-40秒：CPU = 26%, Mem = 513MB, ...
...
时刻 T：CPU = 95%, Mem = 520MB, ...  ← CPU 突然飙升！

6 个连续时间步的模式：
[25, 26, 24, 25, 26, 95]  ← 最后一个突变

模型分析：
"前 5 个点都在 25±2 的范围"
"第 6 个点突然跳到 95"
"这个模式不正常！在我的训练数据中从没见过"

→ 高重构误差，正确检测异常
```

### 滑动窗口的实现

```
初始化：
    window = deque(maxlen=6)
    
采样 1 (t=10s)：
    raw_data = [25, 512, 0.1, 0.05]
    window = [standardized_1]
    len(window) = 1  ← 还不够，继续收集
    
采样 2 (t=20s)：
    raw_data = [26, 513, 0.12, 0.06]
    window = [standardized_1, standardized_2]
    len(window) = 2  ← 还不够
    
采样 3 (t=30s)：
    raw_data = [24, 511, 0.09, 0.04]
    window = [standardized_1, standardized_2, standardized_3]
    len(window) = 3  ← 还不够
    
...
    
采样 6 (t=60s)：
    raw_data = [25, 514, 0.11, 0.05]
    window = [s1, s2, s3, s4, s5, s6]
    len(window) = 6  ← 满了！进行推理
    
    推理完成后：
    
采样 7 (t=70s)：
    raw_data = [95, 520, 2.5, 1.2]  ← CPU 飙升
    window.append(s7)  ← 自动丢掉最旧的 s1
    window = [s2, s3, s4, s5, s6, s7]  ← 新的时间窗口
    进行新的推理
    
这样就保证了模型看到的总是最新的 6 个时间步
```

---

## 📈 阈值的科学设置

### 统计学基础

```
假设模型的重构误差呈正态分布：

              正常数据的误差分布
              
              ╱╲
            ╱    ╲
          ╱        ╲
        ╱            ╲
      ╱___±1σ___±2σ_±3σ╲___
      
      68% 的数据在 ±1σ 内
      95% 的数据在 ±2σ 内
      99.7% 的数据在 ±3σ 内
```

### 为什么使用 mean + 2σ？

```
设 mean = 0.0234, σ = 0.0156

threshold = mean + 2σ = 0.0234 + 2×0.0156 = 0.0546

这个阈值的含义：
"如果数据来自同一个正态分布，
 数据超过这个阈值的概率仅有 2.5%"

换句话说：
- 误报率（正常数据被误判为异常）: 2.5%
- 漏报率（异常数据被误判为正常）: 取决于异常的严重程度

实际调整：
- 如果误报太多 → 提高阈值 (mean + 2.5σ 或更高)
- 如果漏报太多 → 降低阈值 (mean + 1.5σ 或更低)
```

---

## 🚀 实时性能分析

### 延迟组成

```
从获取数据到发出告警的总延迟：

                     总延迟 ≈ 110-130ms
                     
等待数据点     采集时间      处理时间        输出时间
├─ 0-10s   ├─ 50-100ms ├─ 10-20ms    ├─ 1-2ms
│          │           │             │
当前数据点  │Docker API │标化+推理    │日志输出
等到队列   │get_stats()│torch.no_grad│logger.warning()
满6个点    │           │             │
           ↓           ↓             ↓
          采集  →  处理  →  推理  →  告警
          
对于 10 秒采样一次的系统：
- 模型可以在 100ms 内准备好数据
- LSTM 推理在 GPU 上只需 5-10ms
- 告警可以在秒级内发出
- 完全满足实时需求
```

### 内存占用

```
LSTM Autoencoder 模型文件: 2MB
标化器 (scaler):         100KB
滑动窗口 (6×4 数组):      200 bytes
日志缓冲:                 1MB
合计:                    ≈ 3.1MB

这是非常轻量级的边缘计算 (Edge AI)！
可以在任何机器上运行，包括 Raspberry Pi
```

---

## 🎯 异常类型的检测能力

### 能检测到的异常

```
1. 急性异常（突发）
   CPU: 25% → 95%  (30秒内)
   模型看到：[25, 25, 25, 25, 95, 95]
   重构误差：突然飙升 0.234 → 0.85
   ✅ 立即检测

2. 渐进异常（缓慢恶化）
   Mem: 256MB → 1024MB  (1小时)
   采样点之间差异小，但走向异常
   ✅ 检测到整个趋势的异常性

3. 组合异常
   CPU 高 + 内存高 + 网络流量异常
   三个指标同时增长
   ✅ 模型会同时看到这些信号

4. 周期性异常
   每天同一时间都会异常
   如果训练数据没有包含这个周期
   ✅ 会被检测为异常
```

### 不能直接检测的情况

```
❌ 业务合法的峰值（如果没有在训练数据中出现）
   解决：用包含峰值的数据重新训练

❌ 模型训练的容器变更了工作流程
   解决：重新采集数据并重新训练

❌ Docker API 本身出现故障
   解决：添加连接健康检查和重试逻辑
```

---

## 🔒 生产环保建议

### 1. 模型版本管理

```python
# 为模型文件添加版本标签
models/
├── qbittorrent_model_v1.pth       (2024-01-15 训练)
├── qbittorrent_model_v2.pth       (2024-02-01 更新)
└── qbittorrent_model_v2.pkl       (最新版本)

# 定期（每月/每季度）用新数据重新训练
```

### 2. 监控健康检查

```python
def health_check():
    # 检查模型文件是否存在
    assert Path('models/').exists()
    
    # 检查 Docker 连接
    assert collector.client.ping()
    
    # 检查模型权重的梯度
    assert model.parameters() is not None
    
    # 检查滑动窗口状态
    assert len(window) <= 6
    
    return True
```

### 3. 告警去重

```python
# 防止重复告警（如果连续 5 分钟内已有告警，不再重复）
last_alert_time = None

if error > threshold:
    now = time.time()
    if last_alert_time is None or (now - last_alert_time) > 300:
        send_alert()
        last_alert_time = now
```

### 4. 日志轮转

```bash
# 使用 logrotate 防止日志文件过大
cat > /etc/logrotate.d/realtime-monitor <<EOF
/path/to/realtime_monitor.log {
    daily
    rotate 7        # 保留 7 天的日志
    compress        # 压缩旧日志
    missingok
    notifempty
    create 0640
}
EOF
```

---

## 📚 参考资源

- LSTM Autoencoder 论文：https://arxiv.org/abs/1607.00148
- 时序异常检测综述：https://arxiv.org/abs/1811.05269
- Docker Stats API：https://docs.docker.com/engine/api/v1.43/#tag/Container/operation/ContainerStats

---

## 🎓 总结

这个系统展示了：

1. **完整的 ML 工程流程**
   - 从原始数据采集到模型部署
   - 包括数据验证、模型评估、监控告警

2. **时序数据处理的最佳实践**
   - 时间窗口化
   - 标准化和反标准化
   - 保持窗口的同步性

3. **深度学习在实际问题中的应用**
   - LSTM 处理时间依赖性
   - Autoencoder 进行无监督异常检测
   - 在边缘设备上的高效部署

4. **生产级别的可靠性**
   - 持久化存储
   - 实时日志记录
   - 系统健康监测

祝你的项目大成功！🚀
